package teacher_section.class_work.assign_assignment

fun main() {
    val text = "Hello,world!"
    val character = 'l' // Character to remove

    val newText = text.
    replace(character.toString(), "")
    val s=StringBuffer(text)

    println(newText)
}